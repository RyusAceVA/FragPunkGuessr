# =============================================================================
# FragPunk Guessr — variables d'environnement
# Copier vers .env puis remplir. NE JAMAIS commiter le fichier .env.
# =============================================================================

# --- Base de données (PostgreSQL) -------------------------------------------
# Dev local  : lancer `npm run db:dev` puis utiliser l'URL TCP affichée par
#              `npx prisma dev ls`, ex. :
#              postgres://postgres:postgres@localhost:51214/template1?sslmode=disable
# Production : URL de connexion POOLÉE Neon (Vercel Marketplace → Neon),
#              ex. : postgresql://user:pwd@ep-xxx-pooler.eu-central-1.aws.neon.tech/neondb?sslmode=require
DATABASE_URL=""

# --- Application ------------------------------------------------------------
# URL publique du site (en prod : https://ton-projet.vercel.app)
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# Dossier des assets (plans + screenshots), relatif à la racine du projet
# ASSETS_DIR="Maps"

# --- Authentification -------------------------------------------------------
# Secret de signature des sessions — générer avec : npx auth secret
# (ou : node -e "console.log(require('crypto').randomBytes(32).toString('base64'))")
AUTH_SECRET=""

# --- Connexion Google (OPTIONNEL — le bouton n'apparaît que si renseigné) ---
# Google Cloud Console → APIs & Services → Credentials → OAuth client ID
#   Type : Web application
#   Authorized redirect URI : <URL du site>/api/auth/callback/google
#   (en dev : http://localhost:3000/api/auth/callback/google)
# AUTH_GOOGLE_ID=""
# AUTH_GOOGLE_SECRET=""

# --- Bootstrap du PREMIER administrateur (optionnel) ------------------------
# Les comptes vivent en base (table User) : ces deux variables ne servent
# qu'au script `npm run admin:seed` pour créer le premier admin, puis
# peuvent être supprimées. Alternative sans variables :
#   npm run admin:seed -- email "MotDePasse" [username]
ADMIN_EMAIL="admin@exemple.com"
# Hash Argon2id (npm run admin:hash -- "MotDePasse").
# ⚠ Dans un .env, échapper les $ en \$ (le script affiche la ligne prête).
ADMIN_PASSWORD_HASH=""
